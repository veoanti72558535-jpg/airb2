
The user wants to remove all powder-based projectiles (non-pellet, non-slug) from their local projectile database. This is airgun software, so only `pellet` and `slug` projectiles should remain.

Let me check the Projectile type to understand the categorization.
